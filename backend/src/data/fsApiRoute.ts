/**
 * The FileStation 6 API Routes being used in this project. Note that this
 * includes the necessary api parameter for the associated API. These routes are
 * meant to be appended after the /webapi route.
 */
export enum FSApiRoute {
  Auth = "/auth.cgi?api=SYNO.API.Auth&version=3",
}
