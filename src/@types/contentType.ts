/**
 * All of the content types that Polaroids recognizes. For further extension,
 * look up common MIME types, and cross reference with PhotoStation supported types.
 */
export enum ContentType {
  JPG = ".jpg",
  JPEG = ".jpeg",
}
